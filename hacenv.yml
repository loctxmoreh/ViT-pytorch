name: vit
channels:
    - pytorch
dependencies:
    - python=3.8
    - pip
    - pip:
        - torch==1.7.1+cu110.moreh22.12.0
        - torchvision
        - torchaudio
        - packaging                         # for installing apex manually later
        - -r requirements.txt
